--- En CARPETA DEL PROYECTO - CREACION DEL ENTORNO VIRTUAL ---
python -m venv venv

---ESTO SE INSTALA EN LA CARPETA VEN/SCRIPTS DE VENV (ACTIVATE)
activate
pip install virtualenv
pip install tensorflow
pip install pandas
pip install django
